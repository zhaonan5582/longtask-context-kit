---
name: longtask-context-kit
description: >-
  长程/无人值守 agent 任务的抗上下文压缩文档体系（五层分层 + 覆盖式工作快照 + 任务书 + 改库纪律）。
  当任务会跨多轮会话、会话中途会断、需要交接给"下一个自己"或别的 agent、或模型会压缩上下文时使用。
  A layered documentation scaffold for long-running / unattended agent tasks that must survive context
  compaction: live snapshot + task book + indexed method library + case archive, plus discipline for
  changing the library without clobbering previous authors. Use when a task spans many sessions,
  gets interrupted mid-way, needs handover to "the next you" or another agent, or when the model
  compacts context.
agent_created: true
---

# 长任务上下文包（Long-task Context Kit）

## 这套东西解决什么问题

长程 agent 任务有三个绕不开的现实：

1. **上下文会被压缩或截断** —— 而且是**不可控、无序**的：模型分不清"当前小闭环的关键上下文"和"三天前的寒暄"。
2. **会话会中途结束** —— 干到一半就断了，下一轮（或另一个 agent）要能接上。
3. **交接要重读一大堆** —— 常见做法是"读一堆设计文档 + 从头看方法论"，**光是热身就吃掉大半预算**。

后果就是那句最常见的抱怨：**"不是跑通了吗？我上来怎么就卡住了。"**

**本 skill 给的解法**：把"该记什么、记到哪一层、怎么改"制度化，做成**落盘的、分层的、按需深入的**文档体系。

---

## 五层结构

| 层 | 文件 | 装什么 | 谁读、读多少 | 更新频率 |
|---|---|---|---|---|
| **L0.5 快照** | `00_LIVE.md` | **此刻在做什么** | ★ **压缩后第一个读**，全文（≤30 行） | **每个小闭环覆盖重写** |
| **L0 任务书** | `01_TASK.md` | 要干什么 / 铁律 / 坐标 / 读法 / 索引 | **每轮必读**，全文（目标 ≤50 行） | 任务变化时 |
| **L1 索引** | `02_METHOD.md` §0 | 全部条目的**一句话摘要** | 扫一眼，只为定位 | 增条目时 |
| **L2 条目** | `02_METHOD.md` §x.y | 单条方法：症状→根因→判据→修法 | **只读被指到的那一条** | 有新结论时 |
| **L3 案例** | `03_CASES.md` | 长案例 / 日志 / 推演 / 原文 | 只在需要溯源时读 | 随时追加 |

**关键区分**（最容易搞混的一对）：
- **L0 任务书 = "要干什么"**（变化慢，任务级）
- **L0.5 快照 = "干到哪了"**（变化快，每小闭环一次）

**分层判据**（内容该放哪层）：
- 一句话能说清、**每次都要遵守** → **L0 铁律**
- 一句话能说清、**只在特定情形才需要** → **L1 索引 + L2 条目**
- 需要举例、贴日志、讲推演 → **L3**（L2 只留结论 + 一个锚点）

---

## 三条核心纪律（最值钱的部分）

### 1. 快照必须"覆盖式"，且不许过期

`00_LIVE.md` **不是日志**——**覆盖重写，永远只反映此刻**，且 **≤30 行**（超了就删旧的）。

> **过期的快照比没有快照更坏**：它会让人照着已经作废的状态干活。

### 2. 改库必须"有理有据"，不同情况**并存**而不是覆盖

面对已有条目，**不许"遇到个问题回来哐哐就改库"**。改前必做三件事：

1. **先查有没有**（grep 索引 + 术语）；
2. 有则**先问"前人为什么这么写"**——不读完旧条目不许动笔；
3. **判性质**：

| 判定 | 处置 |
|---|---|
| **真不对**（被实证推翻） | 改，但**留痕**：`~~旧说法~~ → 新说法`，附 `依据：<日志/复现/比对>` |
| **不同情况**（旧说法在自己条件下成立） | **【并存】** —— 加"适用条件 / 反例"分节，**绝不覆盖** |

> 直接覆盖 = 把别人的边界条件一起抹掉，下一个人还得再踩一遍。
> 另外：**必须有实证**，禁止凭一次印象改、禁止把个别情形写成通则。

### 3. "跳过 ≠ 通过"

任何"因为条件不具备而跳过"的用例，必须在台账里标成 **`NOT-TESTED`（未测）**，
**绝不能标成 `PASS`（通过）**。

> 跳过项会在**统计口径里消失**，于是"覆盖率 94/97""主流程已跑通"这类结论
> **在纸面上成立、在真机上崩塌**。

---

## 配套的验收口径

**写进去 → 能查到 → 下一环节看得见**（三段证据齐全才算通过）。

- "接口返回 200 / 某条路径能走" **都不算跑通**；
- 验收必须**用全新数据**（不许拿"很久以前那条已经跑通的老数据"自证）；
- 报告覆盖率时**必须同时报 `NOT-TESTED` 的数量和清单**。

---

## 安装 / 使用

**一键生成骨架**（在目标项目的 docs 目录下）：

```bash
python scripts/init_task_docs.py <docs_dir> "<一句话任务描述>"
```

会生成 `00_LIVE.md` / `01_TASK.md` / `02_METHOD.md` / `03_CASES.md` / `04_RULES.md` 五个文件（已填好结构与占位说明）。

**然后**（关键，否则机制不会自己转起来）：

1. 把 **"每轮开工先读 `00_LIVE.md` → 再读 `01_TASK.md` → 只读指到的那节"**
   写进你的 agent 任务描述 / automation prompt；
2. 把 **"每轮收尾必须覆盖重写 `00_LIVE.md`"** 同样写进 prompt；
3. 把 **"文档体系在哪、压缩后先读哪个"** 写进 agent 的长期记忆（如 `MEMORY.md`）——
   这样**即使上下文被压缩，它也能自己找回来**。

---

## 目录内容

```
SKILL.md                     本文件
scripts/init_task_docs.py    一键生成五层骨架
templates/00_LIVE.md         快照模板（覆盖式）
templates/01_TASK.md         任务书模板（含铁律位）
templates/02_METHOD.md       方法论主文件模板（含 §0 索引位）
templates/03_CASES.md        案例 L3 模板
templates/04_RULES.md        库规范 / 改库纪律模板
README.md                    给 Git 仓库用的说明
```

---

## 适用 / 不适用

**适用**：跨多轮会话的长任务；无人值守（定时触发新会话）；多 agent 接力；
需要给"下一个自己"留交接单；上下文预算紧张、必须最小化"热身"成本。

**不适用**：一次能做完的短任务（这套结构的维护成本大于收益）。
