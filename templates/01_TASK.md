# 任务书（**每轮必读**，目标 ≤50 行）

> **用法**：只读本文件；它指到哪一节，再去读那一节。**不要通读任何大文档。**
> 最后更新：**{{DATE}}**

---

## 0. 一句话任务

{{TASK}}

## 0.5 现状校正（重要）

> 记录"之前宣称完成、实际并不彻底"的地方 —— **防止后来者照着乐观结论干活**。
> （示例：某功能"已跑通"其实是拿老数据自证的；用全新数据一验就卡。）

## 1. 下一步（按序，做完一条划掉）

1. …

## 2. 铁律（违反即失误）

1. **没跑完不许汇报**；"接口 200 / 某条路径能走"都不算跑通。
2. **必须用全新数据验收**，不许拿老数据链自证。
3. **一轮只做一个小闭环**，做完立刻落盘。
4. **改了代码必须当轮内编译 + 实测**；**绝不允许"改了没测"就结束**。
5. **每轮收尾必须覆盖重写 `00_LIVE.md`**（给下一个你的交接单）。
6. **跳过必须标 `NOT-TESTED`，绝不能标 `PASS`**（跳过 ≠ 通过）。
7. **改库有据**：先查有没有 → 问前人为什么这么写 → 真不对才改且留痕；不同情况**【并存】**。
8. （按你的项目补充：对外请求红线 / 只改哪些目录 / 服务怎么起 / 凭据怎么放 …）

## 3. 最小环境坐标

```
代码根   …
数据库   …
日志     …
工具/探针 …
运行时   …
起服务   …（★ 用受管后台任务；`nohup … &` 之类的会被回收）
```

## 4. 文档体系怎么读（分层，别通读）

```
L0.5 00_LIVE.md    ★ 压缩后第一个读（≤30 行，覆盖式）
L0   01_TASK.md    每轮必读（本文件）
L1   02_METHOD.md §0 索引   扫一眼定位
L2   02_METHOD.md §x.y      只读被指到的那条
L3   03_CASES.md   需要溯源时才读
```

## 5. 文档地图（哪些在用、哪些别看）

| 文档 | 状态 | 说明 |
|---|---|---|
| `00_LIVE.md` | ★★★ 压缩后第一个读 | 覆盖式状态快照 |
| `01_TASK.md` | ★ 每轮必读 | 本文件 |
| `02_METHOD.md` | ★ 在用 | 方法论（§0 索引，按索引查） |
| `03_CASES.md` | ★ 在用 | L3 备查 |
| `04_RULES.md` | ★ 在用 | 改库纪律 |
| （已作废的文档） | ❌ 已废弃 | 仅作溯源 |

## 6. 收工条件

范围全部做完 + 问题修完后，**整体再过三遍**；
**某遍发现并修改了问题 ⇒ 「连续无修改」计数归零、从 1 重新开始**；
直到**连续三遍完全没有可修改之处**才允许最终汇报（须列未完成项与下一步，禁止邀功）。

### ★★★ 完成态约定（达到"三遍无修改"之后）

1. 在**本文件最顶部**加一行：`> # ★★★ 【已完成】YYYY-MM-DD HH:MM · 连续三遍无修改`
2. 把 `00_LIVE.md` 改写成"已完成态"（任务已完成 / 最终结论 / 遗留项）
3. 做最终汇报（列未完成项，禁止邀功）
4. **此后每轮开工读到【已完成】⇒ 只确认无新问题，然后立即结束**：
   不重复劳动、不为"显得有产出"而改、不把"完成"改回"未完成"；**真发现新问题才去掉那行**。

> 原因：**自动化不会自己停**。空转是浪费，**乱改是破坏**。

## 更新日志

- {{DATE}} · 建立
